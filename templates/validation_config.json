{
  "schema_version": "raes-validation-config/1",
  "status": "draft_not_executable",
  "live_enabled": false,
  "stage": "{{S5_OR_S9}}",
  "frame": {
    "path": "{{FROZEN_FRAME_FILE}}",
    "sha256": "{{SHA256}}",
    "unit": "{{UNIT}}"
  },
  "rules": {
    "production_codebook": "{{PATH}}",
    "audit_codebook": "{{PATH_USED_BY_AUDITOR_AND_ADJUDICATOR}}",
    "eligibility_sha256": "{{SHA256}}"
  },
  "reviewers": {
    "executor": {
      "provider": "{{PROVIDER}}",
      "model_snapshot": "{{EXACT_MODEL_ID}}",
      "settings": {}
    },
    "auditor": {
      "provider": "{{DIFFERENT_PROVIDER_WHERE_POSSIBLE}}",
      "model_snapshot": "{{EXACT_MODEL_ID}}",
      "settings": {}
    },
    "adjudicator": {
      "provider": "{{PROVIDER}}",
      "model_snapshot": "{{EXACT_MODEL_ID}}",
      "settings": {}
    }
  },
  "sampling": {
    "mode": "{{CENSUS_OR_DESIGNED_SAMPLE}}",
    "strata": [],
    "seed": null,
    "round_size": null,
    "stopping_rule": "{{APPROVED_RULE}}"
  },
  "routing": {
    "screening": "{{TWO_PRIMARY_THEN_THIRD_IF_NEEDED_OR_STAGE_SPECIFIC_RULE}}",
    "coding": "auditor challenges -> blinded adjudicator -> exact agreement, otherwise bounded human adjudication"
  },
  "payload_policy": {
    "downstream_statistics_forbidden": true,
    "source_text_is_untrusted_data": true,
    "adjudicator_never_sees_auditor_proposal": true
  },
  "retries": {
    "max_new_attempts_per_invocation": null,
    "resume_global_attempt_ids": true,
    "technical_failure_is_not_a_decision": true,
    "request_identity_unchanged": true
  },
  "budget": {
    "currency": "USD",
    "approved_max": null,
    "approval_record": null
  },
  "artifacts": {
    "raw_responses": "{{PATH}}",
    "normalized_responses": "{{PATH}}",
    "reconciliation": "{{PATH}}",
    "hash_manifest": "{{PATH}}"
  },
  "note": "Design skeleton. Fill in and review before any live run; this file does not call any provider."
}
