Paper identifier: {{PAPER_ID}}

Source bundle (approved files/text only):
{{SOURCES_JSON}}

Apply the codebook to every eligible condition. Creating an eligible row and having
enough information for an effect are separate decisions. Keep incomplete eligible
rows and explain which field is missing. Do not import comparators from elsewhere.
Do not split, merge or multiply independent units beyond the written aggregation rule.

Use the executor column contract from the system message. Return one JSON object,
not prose or Markdown. Preserve source units and canonical outcome directions.
In self_check, state coverage of eligible conditions, missing inputs, comparator
pairing, provenance and code-owned-field exclusion. This self-check is not an audit.
