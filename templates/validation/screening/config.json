{
  "schema_version": "raes-screening-validation-config/1",
  "purpose": "Operational settings of the screening audit (stage S5 in the protocol). Fill every {{...}}; the examples show one way, not the required one.",
  "status": "draft_not_executable",
  "live_enabled": false,
  "snapshot": "{{SEARCH_SNAPSHOT_ID}}",
  "screening_rules_version": "{{RULES_VERSION}}",
  "audit_codebook": {
    "path": "validation/screening/codebook.json",
    "sha256": "{{SHA256}}"
  },
  "eligibility_sha256": "{{SHA256}}",
  "order": "{{e.g. full_text first, then abstract}}",
  "full_text": {
    "frame": {"path": "{{FROZEN_FULL_TEXT_EXCLUSIONS}}", "sha256": "{{SHA256}}", "size": null},
    "strata": "{{e.g. near_miss = failed exactly one criterion; other = the rest}}",
    "sample": {"round_size": null, "allocation": "{{e.g. by stratum}}", "seed": null},
    "reviewers": {
      "{{ROLE, e.g. auditor_1}}": {"provider": "{{PROVIDER}}", "model_snapshot": "{{EXACT_MODEL_ID}}", "settings": {}},
      "{{ROLE, e.g. auditor_2}}": {"provider": "{{DIFFERENT_PROVIDER}}", "model_snapshot": "{{EXACT_MODEL_ID}}", "settings": {}},
      "{{ROLE, e.g. third}}": {"provider": "{{PROVIDER}}", "model_snapshot": "{{EXACT_MODEL_ID}}", "settings": {}}
    },
    "routing": "{{e.g. auditor_1 and auditor_2 independently; third only when both answers are valid and disagree; majority by code; human adjudication only when the majority says include}}",
    "stopping_rule": "{{APPROVED_RULE}}"
  },
  "abstract": {
    "frame": {"path": "{{FROZEN_ABSTRACT_EXCLUSIONS}}", "sha256": "{{SHA256}}", "size": null},
    "strata": "{{e.g. search batches}}",
    "sample": {"round_size": null, "allocation": "{{e.g. proportional to stratum size}}", "seed": null},
    "reviewers": {
      "{{ROLE, e.g. auditor}}": {"provider": "{{PROVIDER}}", "model_snapshot": "{{EXACT_MODEL_ID}}", "settings": {}}
    },
    "candidate_route": "{{e.g. retrieve the full text; run the frozen full-text screen; if it includes the record, review it with the full_text reviewers}}",
    "stopping_rule": "{{APPROVED_RULE, e.g. a round with no candidate passing the frozen full-text screen ends the audit; rounds in which candidates pass but the reviewers exclude all of them count toward a cumulative total; a confirmed miss is added back and the audit continues}}"
  },
  "rule_change": "{{e.g. rules stay fixed during an audit; a change is a new version, the current run is archived and a fresh sample is frozen}}",
  "payload_policy": {
    "program_decision_hidden": true,
    "other_answers_hidden": true,
    "downstream_results_hidden": true,
    "source_text_is_untrusted_data": true
  },
  "retries": {
    "max_new_attempts_per_invocation": null,
    "resume_global_attempt_ids": true,
    "technical_failure_is_not_a_decision": true,
    "request_identity_unchanged": true
  },
  "budget": {"currency": "USD", "approved_max": null, "approval_record": null},
  "artifacts": {
    "raw_responses": "{{PATH}}",
    "normalized_responses": "{{PATH}}",
    "candidate_resolution": "{{PATH}}",
    "hash_manifest": "{{PATH}}"
  },
  "note": "Design skeleton. Fill in and review before any live run; this file does not call any provider."
}
